import $ from "./Header.module.scss";

export const Header = () => (
  <header className={$.header}>
    // TODO: add hamburger menu and logo
    <div>hamburger menu</div>
    <div>logo</div>
  </header>
);
